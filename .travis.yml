language: python
python:
  - "3.6"
# command to install dependencies
services:
- postgresql
before_script:
- psql -c 'create database test_db;' -U postgres
env:
  global:
    - DATABASE_URL=postgresql://postgres:winston77@localhost/flask_api
    - SECRET= asdjfksdakfjlasdfkwerefrewfoweo
install:
  - pip install -r requirements.txt
  - pip install coveralls
# command to run tests
script: 
 - pytest
 - nosetests --with-coverage 
after_success: 
 - coveralls
 - coverage report